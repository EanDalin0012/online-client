import { ResourceID } from './resource-id';
export class ProductViewDetails {
    id: string;
    context_ch: string;
    context_en: string;
    context_kh: string;
    rescource_list = new Array<ResourceID>()
}