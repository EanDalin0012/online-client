import { deviceInfo } from '../../constants/common.const';
import { DeviceDetectorInfo } from './device-detector-info';
export class LoadUserInfo {
  userName: string;
  deviceInfo: DeviceDetectorInfo;
  networkIP: string;
}
